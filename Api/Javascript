function doPost(e) {
  try {
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("Leads");
    const data = JSON.parse(e.postData.contents);
    const userEmail = data.email;
    const actionType = data.type === 'download' ? "4K Video Download" : "Branded GIF";

    // 1. Log the Lead
    sheet.appendRow([new Date(), userEmail, data.type, "Free User"]);

    // 2. The Sales Funnel Email (Auto-Responder)
    GmailApp.sendEmail(userEmail, "Your Asset is Ready! 🚀 (Plus a gift inside)", "", {
      name: "SS Solutions Premium",
      htmlBody: `
        <div style="font-family: 'Helvetica', sans-serif; color: #1e293b; max-width: 600px; margin: auto; border: 1px solid #e2e8f0; border-radius: 12px; overflow: hidden;">
          <div style="background: #0f172a; padding: 30px; text-align: center;">
            <h1 style="color: #38bdf8; margin: 0; font-size: 24px;">SS Solutions Asset Engine</h1>
          </div>
          <div style="padding: 30px; line-height: 1.6;">
            <p>Hello,</p>
            <p>Your <b>${actionType}</b> has been successfully processed! Thanks for using the engine.</p>
            
            <div style="background: #f8fafc; border: 1px dashed #38bdf8; padding: 20px; border-radius: 8px; margin: 20px 0; text-align: center;">
              <h3 style="margin-top: 0; color: #0f172a;">🚀 UNLOCK PRO ACCESS</h3>
              <p style="font-size: 14px;">Tired of waiting? Support the project with a <b>$5 Pro Pass</b> and get:</p>
              <ul style="text-align: left; font-size: 13px; display: inline-block;">
                <li>✅ Unlimited 4K Ultra-HD Downloads</li>
                <li>✅ No Lead-Gen Popups (Ever)</li>
                <li>✅ Priority Processing Speed</li>
              </ul>
              <br><br>
              <a href="https://buy.stripe.com" 
                 style="background: #6366f1; color: white; padding: 12px 25px; text-decoration: none; border-radius: 8px; font-weight: bold; display: inline-block;">
                 Get My Pro Pass Now
              </a>
            </div>

            <p style="font-size: 12px; color: #64748b;">If you have any issues with your download, just reply to this email.</p>
            <p style="font-size: 12px; color: #64748b;">Best,<br><b>The SS Solutions Team</b></p>
          </div>
        </div>
      `
    });

    return ContentService.createTextOutput(JSON.stringify({ "result": "success" }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (err) {
    return ContentService.createTextOutput(JSON.stringify({ "result": "error", "error": err.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

